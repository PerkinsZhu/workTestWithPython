KNN记录
    1、距离算法有哪几种？分别适用于那种场景？
    2、正确率的计算方法有哪些？数据测试方式?
    3、数值归一化方法有哪些，各种方法使用的场景又是什么？ http://blog.csdn.net/mpbchina/article/details/7573519，有量纲到无量纲数据
决策树
    1、如何选取bestFuture? ID3、C4.5、CART
朴素贝叶斯：
    1、如何解决下溢出问题？使用自然对数把乘转化为加  log(a·b) = log(a)+log(b)
    2、词集和词袋的区别？词集：每个单词只能出现一次 词袋：每个单词可以出现多次。词袋统计的是测试数据集中单词出现的次数。
    3、留存交叉验证是什么？
    4、TF-IDF相关算法？
    5、停用词表 移除频率
logistic回归
    1、如何处理缺失值。数据集中部分数据缺失、无效、导致的数据集不完整
    2、如何处理多分类问题？
    处理步骤：1、使用随机梯度下降算法 求出回归函数（也即是确定回归系数） 2、使用sigmoid函数进行分类
SVM支持向量机
    1、核函数的目的？把数据向高维空间映射，以便能够找到进行划分的超平面，然后再降维映射到低维空间，得到一个曲线，
    2、二范数是什么意思？
    【暂停】
AdaBoost
    1、自举汇聚法的原理和目的是做什么？
    2、正确率、召回率和ROC的意义及其计算方法？
    3、在寻找最优弱分类器的时候，为什么会有如下规则？理解原因：
          如果第i个样本被正确分类了，那么在下一次迭代过程中，该样本的权重DiI就会降低。
          如果第i个样本没有被正确分类，那么在下次迭代过程中，该样本的权重Di就会升高。

使用sklearn实现如上算法模型